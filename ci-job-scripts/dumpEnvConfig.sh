#!/bin/bash

echo "#############################################"
docker images
echo "#############################################"
docker images --filter "dangling=true" -q --no-trunc
echo "#############################################"
echo
echo "#############################################"
docker ps -a
echo "#############################################"
echo
echo "#############################################"
echo
echo "#############################################"
env | grep GERRIT
echo "#############################################"
env | grep GIT 
echo "#############################################"
env | grep JENKINS
echo "#############################################"
env | grep BUILD
echo "#############################################"
echo
env
echo
